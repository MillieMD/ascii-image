# ascii-image

Read a 24-bit Bitmap image, and print to stdout as black and white ASCII art

## Usage

```
ascii-image [-h] img
```

Currently only supports 24-bit colour Bitmaps.

## Installation

Run `install.bat` *or* run `pip3 install -e .` from the root directory of the repository, to install as a CLI tool.
